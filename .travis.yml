language: python
python:
  - '3.9'

before_script:
  pip install -r requirements.txt

branches:
  only:
    - staging

env: DJANGO_SETTINGS_MODULE="purbeurre.purbeurre.settings.travis"

services:
  - postresql

script:
  - purbeurre/manage.py test